# UCC — Claude Code project notes

## Git workflow

- **Default PR base branch: `dev`** — always open pull requests against `dev`, not `main`.
- Feature branches follow the `claude/<description>-<id>` naming convention.
